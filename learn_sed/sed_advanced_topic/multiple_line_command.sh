#!/bin/bash

:'
    n: move next line (1 line) to stream to create a multiple line group for processing
        when you do this you are processing the next line
    N: add next line (1 line) to stream to create a multiple line group for processing
        when you do this you are processing the current line combining next line
    D: Delete a single line in multiple line group
    P: Print a single line in a multiple line group
'


